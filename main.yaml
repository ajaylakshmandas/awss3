name: Upload Website

on:
  push:
    branches:
    - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: jakejarvis/s3-sync-action@master
      with:
        args: --acl public-read --follow-symlinks --delete --exclude '.git*/*'
      env:
        AWS_S3_BUCKET: ${{ secrets.ajaydummy }}
        AWS_ACCESS_KEY_ID: ${{ secrets.AKIATQRZYLA65DEGNU2E}}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.LDdcz7J+i3d4tmIvblj60H/8IrSiq8SIicjiGxMr}}
